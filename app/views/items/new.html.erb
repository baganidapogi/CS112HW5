<% provide(:title, 'New Item') %>
<h1>New Item</h1>

<%= simple_form_for(@item) do |f| %>
  <%= f.input :name %>
  <%= f.input :code %>
  <%= f.input :description %>
  <%= f.input :price %>
  <hr>
  <%= f.submit "Save Record" %>
<% end %>

<%= link_to "Back to items", items_path %>
